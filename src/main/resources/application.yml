server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jhub
    username: sa
    password:

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update
    #    show-sql: true
    properties:
      hibernate:
        default_batch_fetch_size: 1000
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  #  org.hibernate.type.descriptor: trace


  #org.hibernate.type: trace
