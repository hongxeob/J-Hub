server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jhub
    username: sa
    password:
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create-drop

  show-sql: true
  properties:
    hibernate:
      show_sql: true
      format_sql: true

      logging.level:
  org.hibernate.SQL: debug

  #org.hibernate.type: trace
